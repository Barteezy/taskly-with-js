<h1>Add Task</h1>

<%= form_for [@task_list, @task] do |f| %>
    <div>
      <%= f.label :description %>
      <%= f.text_field :description %>
    </div>
    <div>
      <%= f.submit "Add Task" %>
    </div>
<% end %>